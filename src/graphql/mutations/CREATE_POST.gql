import gql from "graphql-tag"

export default gql`
    mutation CreatePost($post: PostInput) {
        createPost(post: $post){
            title
            body
            type
            iconId
            pinned
        }
    }`