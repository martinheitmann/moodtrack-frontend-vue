import gql from "graphql-tag"

export default gql`
    mutation UploadIcon($file: Upload!) {
        uploadIcon(file: $file){
            filename
            mimetype
            encoding
        }
    }
`