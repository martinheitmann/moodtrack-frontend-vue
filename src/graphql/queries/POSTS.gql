import gql from "graphql-tag"

export default gql`
    query Posts {
        posts {
            _id
            created
            title
            body
            type
            iconId
            pinned
        }
    }
`